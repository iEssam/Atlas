<ContentDialog
    x:Class="Atlas.App.Views.PluginGrantDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:Atlas.App.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    PrimaryButtonText="Save grant"
    CloseButtonText="Cancel"
    DefaultButton="Primary">

    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="620">
        <StackPanel Spacing="14" MinWidth="460" MaxWidth="540" Margin="0,4,0,0">

            <TextBlock Text="PLUGIN ACCESS / READ ONLY"
                       Style="{StaticResource AtlasSectionLabelStyle}" />

            <TextBlock x:Name="HeaderText" Style="{StaticResource BodyStrongTextBlockStyle}"
                       TextWrapping="Wrap" />

            <TextBlock TextWrapping="Wrap"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Text="Each capability is a read-only slice of Atlas data. This plugin can read only what you tick here and nothing else - grant as little as it needs. Untick everything to leave it with no access at all." />

            <ItemsControl x:Name="CapabilityList"
                          AutomationProperties.Name="Granted capabilities"
                          Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                          Padding="12">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:CapabilityChoiceViewModel">
                        <CheckBox IsChecked="{x:Bind IsSelected, Mode=TwoWay}"
                                  AutomationProperties.Name="{x:Bind Label}"
                                  Margin="0,2">
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <FontIcon Glyph="{x:Bind Glyph}" FontSize="15" VerticalAlignment="Center"
                                          Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <StackPanel>
                                    <TextBlock Text="{x:Bind Label}" />
                                    <TextBlock Text="{x:Bind Description}"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               TextWrapping="Wrap" />
                                </StackPanel>
                            </StackPanel>
                        </CheckBox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</ContentDialog>
